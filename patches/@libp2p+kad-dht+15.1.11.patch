diff --git a/node_modules/@libp2p/kad-dht/dist/src/content-fetching/index.js b/node_modules/@libp2p/kad-dht/dist/src/content-fetching/index.js
index ed65ccc..fbf265b 100644
--- a/node_modules/@libp2p/kad-dht/dist/src/content-fetching/index.js
+++ b/node_modules/@libp2p/kad-dht/dist/src/content-fetching/index.js
@@ -56,7 +56,13 @@ export class ContentFetching {
      */
     async *sendCorrectionRecord(key, vals, best, options) {
         this.log('sendCorrection for %b', key);
+        const keyStr = new TextDecoder().decode(key);
+        const isUsernameKey = keyStr.startsWith('/kiyeovo-username/');
         const fixupRec = createPutRecord(key, best);
+        const fixupRecordValue = Libp2pRecord.deserialize(fixupRec).value;
+        if (isUsernameKey) {
+            console.warn(`[USERNAME-CORRECTION][START] key=${keyStr} candidateValues=${vals.length}`);
+        }
         for (const { value, from } of vals) {
             // no need to do anything
             if (uint8ArrayEquals(value, best)) {
@@ -77,21 +83,48 @@ export class ContentFetching {
             }
             // send correction
             let sentCorrection = false;
+            let queryErrorCount = 0;
+            let peerResponseCount = 0;
+            let mismatchedResponseCount = 0;
             const request = {
                 type: MessageType.PUT_VALUE,
                 key,
                 record: fixupRec
             };
             for await (const event of this.network.sendRequest(from, request, options)) {
-                if (event.name === 'PEER_RESPONSE' && (event.record != null) && uint8ArrayEquals(event.record.value, Libp2pRecord.deserialize(fixupRec).value)) {
-                    sentCorrection = true;
+                if (event.name === 'QUERY_ERROR') {
+                    queryErrorCount++;
+                    if (isUsernameKey) {
+                        const errText = event.error instanceof Error ? event.error.message : String(event.error);
+                        console.warn(`[USERNAME-CORRECTION][QUERY_ERROR] key=${keyStr} target=${from.toString()} path=${event.path.index}/${event.path.total} err=${errText}`);
+                    }
+                }
+                if (event.name === 'PEER_RESPONSE') {
+                    peerResponseCount++;
+                    const matches = event.record != null && uint8ArrayEquals(event.record.value, fixupRecordValue);
+                    if (matches) {
+                        sentCorrection = true;
+                    }
+                    else {
+                        mismatchedResponseCount++;
+                    }
+                    if (isUsernameKey) {
+                        console.warn(`[USERNAME-CORRECTION][PEER_RESPONSE] key=${keyStr} target=${from.toString()} path=${event.path.index}/${event.path.total} hasRecord=${event.record != null} matchesFixup=${matches}`);
+                    }
                 }
                 yield event;
             }
             if (!sentCorrection) {
-                throw new QueryError('Could not send correction');
+                // Do not fail read path if correction cannot be sent; the best value is still usable.
+                this.log.error('Failed error correcting entry');
+                if (isUsernameKey) {
+                    console.error(`[USERNAME-CORRECTION][FAILED] key=${keyStr} target=${from.toString()} peerResponses=${peerResponseCount} mismatchedResponses=${mismatchedResponseCount} queryErrors=${queryErrorCount}`);
+                }
+                continue;
+            }
+            if (isUsernameKey) {
+                console.warn(`[USERNAME-CORRECTION][OK] key=${keyStr} target=${from.toString()} peerResponses=${peerResponseCount} queryErrors=${queryErrorCount}`);
             }
-            this.log.error('Failed error correcting entry');
         }
     }
     /**
diff --git a/node_modules/@libp2p/kad-dht/dist/src/index.d.ts b/node_modules/@libp2p/kad-dht/dist/src/index.d.ts
index a8a01b8..2b6d186 100644
--- a/node_modules/@libp2p/kad-dht/dist/src/index.d.ts
+++ b/node_modules/@libp2p/kad-dht/dist/src/index.d.ts
@@ -495,6 +495,10 @@ export interface KadDHTInit {
      * Record validators
      */
     validators?: Validators;
+    /**
+     * Optional version-aware update validator.
+     */
+    validateUpdate?: (key: Uint8Array, existing: Uint8Array, incoming: Uint8Array) => Promise<void>;
     /**
      * How often to query our own PeerId in order to ensure we have a
      * good view on the KAD address space local to our PeerId
diff --git a/node_modules/@libp2p/kad-dht/dist/src/kad-dht.js b/node_modules/@libp2p/kad-dht/dist/src/kad-dht.js
index 79b18c0..d8bcc42 100644
--- a/node_modules/@libp2p/kad-dht/dist/src/kad-dht.js
+++ b/node_modules/@libp2p/kad-dht/dist/src/kad-dht.js
@@ -221,6 +221,7 @@ export class KadDHT extends TypedEventEmitter {
             providers: this.providers,
             peerRouting: this.peerRouting,
             validators: this.validators,
+            validateUpdate: init.validateUpdate,
             logPrefix,
             metricsPrefix,
             datastorePrefix,
diff --git a/node_modules/@libp2p/kad-dht/dist/src/network.js b/node_modules/@libp2p/kad-dht/dist/src/network.js
index 559b220..5897253 100644
--- a/node_modules/@libp2p/kad-dht/dist/src/network.js
+++ b/node_modules/@libp2p/kad-dht/dist/src/network.js
@@ -3,7 +3,7 @@ import { Libp2pRecord } from '@libp2p/record';
 import { AdaptiveTimeout } from '@libp2p/utils/adaptive-timeout';
 import { pbStream } from 'it-protobuf-stream';
 import { TypedEventEmitter } from 'main-event';
-import { Message } from './message/dht.js';
+import { Message, MessageType } from './message/dht.js';
 import { fromPbPeerInfo } from './message/utils.js';
 import { sendQueryEvent, peerResponseEvent, queryErrorEvent, dialPeerEvent } from './query/events.js';
 /**
@@ -196,6 +196,16 @@ export class Network extends TypedEventEmitter {
         catch (err) {
             this.metrics.errors?.increment({ [type]: true });
             stream?.abort(err);
+            let keyStr = '';
+            try {
+                keyStr = msg.key != null ? new TextDecoder().decode(msg.key) : '';
+            }
+            catch { }
+            if (type === MessageType.PUT_VALUE && keyStr.startsWith('/kiyeovo-username/')) {
+                const errText = err instanceof Error ? err.message : String(err);
+                const elapsedMs = Date.now() - signal.start;
+                console.error(`[USERNAME-CORRECTION][SEND_REQUEST_ERROR] to=${to.toString()} key=${keyStr} path=${options.path.index}/${options.path.total} timeoutMs=${signal.timeout} elapsedMs=${elapsedMs} aborted=${signal.aborted} err=${errText}`);
+            }
             yield queryErrorEvent({ from: to, error: err, path: options.path }, options);
         }
         finally {
diff --git a/node_modules/@libp2p/kad-dht/dist/src/rpc/handlers/put-value.d.ts b/node_modules/@libp2p/kad-dht/dist/src/rpc/handlers/put-value.d.ts
index c75bc4e..2add8e2 100644
--- a/node_modules/@libp2p/kad-dht/dist/src/rpc/handlers/put-value.d.ts
+++ b/node_modules/@libp2p/kad-dht/dist/src/rpc/handlers/put-value.d.ts
@@ -7,6 +7,7 @@ export interface PutValueHandlerInit {
     validators: Validators;
     logPrefix: string;
     datastorePrefix: string;
+    validateUpdate?: (key: Uint8Array, existing: Uint8Array, incoming: Uint8Array) => Promise<void>;
 }
 export interface PutValueHandlerComponents {
     datastore: Datastore;
@@ -15,6 +16,7 @@ export interface PutValueHandlerComponents {
 export declare class PutValueHandler implements DHTMessageHandler {
     private readonly components;
     private readonly validators;
+    private readonly validateUpdate?;
     private readonly log;
     private readonly datastorePrefix;
     constructor(components: PutValueHandlerComponents, init: PutValueHandlerInit);
diff --git a/node_modules/@libp2p/kad-dht/dist/src/rpc/handlers/put-value.js b/node_modules/@libp2p/kad-dht/dist/src/rpc/handlers/put-value.js
index 2db17ed..a33e58e 100644
--- a/node_modules/@libp2p/kad-dht/dist/src/rpc/handlers/put-value.js
+++ b/node_modules/@libp2p/kad-dht/dist/src/rpc/handlers/put-value.js
@@ -5,14 +5,16 @@ import { bufferToRecordKey } from '../../utils.js';
 export class PutValueHandler {
     components;
     validators;
+    validateUpdate;
     log;
     datastorePrefix;
     constructor(components, init) {
-        const { validators } = init;
+        const { validators, validateUpdate } = init;
         this.components = components;
         this.log = components.logger.forComponent(`${init.logPrefix}:rpc:handlers:put-value`);
         this.datastorePrefix = `${init.datastorePrefix}/record`;
         this.validators = validators;
+        this.validateUpdate = validateUpdate;
     }
     async handle(peerId, msg) {
         const key = msg.key;
@@ -27,6 +29,20 @@ export class PutValueHandler {
             await verifyRecord(this.validators, deserializedRecord);
             deserializedRecord.timeReceived = new Date();
             const recordKey = bufferToRecordKey(this.datastorePrefix, deserializedRecord.key);
+            if (this.validateUpdate != null) {
+                let existingRaw;
+                try {
+                    existingRaw = await this.components.datastore.get(recordKey);
+                    const existing = Libp2pRecord.deserialize(existingRaw);
+                    await this.validateUpdate(deserializedRecord.key, existing.value, deserializedRecord.value);
+                }
+                catch (err) {
+                    if (err.message === 'stale record rejected') {
+                        this.log('rejected stale record for %b (version too old)', key);
+                        return existingRaw != null ? { ...msg, record: existingRaw } : msg;
+                    }
+                }
+            }
             await this.components.datastore.put(recordKey, deserializedRecord.serialize().subarray());
             this.log('put record for %b into datastore under key %k', key, recordKey);
         }
diff --git a/node_modules/@libp2p/kad-dht/dist/src/rpc/index.d.ts b/node_modules/@libp2p/kad-dht/dist/src/rpc/index.d.ts
index 2c4606c..f86b850 100644
--- a/node_modules/@libp2p/kad-dht/dist/src/rpc/index.d.ts
+++ b/node_modules/@libp2p/kad-dht/dist/src/rpc/index.d.ts
@@ -16,6 +16,7 @@ export interface RPCInit {
     providers: Providers;
     peerRouting: PeerRouting;
     validators: Validators;
+    validateUpdate?: (key: Uint8Array, existing: Uint8Array, incoming: Uint8Array) => Promise<void>;
     logPrefix: string;
     metricsPrefix: string;
     datastorePrefix: string;
diff --git a/node_modules/@libp2p/kad-dht/src/index.ts b/node_modules/@libp2p/kad-dht/src/index.ts
index 4f556be..12e2f78 100644
--- a/node_modules/@libp2p/kad-dht/src/index.ts
+++ b/node_modules/@libp2p/kad-dht/src/index.ts
@@ -554,6 +554,13 @@ export interface KadDHTInit {
    */
   validators?: Validators
 
+  /**
+   * Optional version-aware update validator. Called on PUT_VALUE when a record
+   * already exists for the key. Throws to reject the incoming record (e.g. if
+   * its version is lower than the existing one).
+   */
+  validateUpdate?: (key: Uint8Array, existing: Uint8Array, incoming: Uint8Array) => Promise<void>
+
   /**
    * How often to query our own PeerId in order to ensure we have a
    * good view on the KAD address space local to our PeerId
diff --git a/node_modules/@libp2p/kad-dht/src/kad-dht.ts b/node_modules/@libp2p/kad-dht/src/kad-dht.ts
index 6d9aded..4bf2b17 100644
--- a/node_modules/@libp2p/kad-dht/src/kad-dht.ts
+++ b/node_modules/@libp2p/kad-dht/src/kad-dht.ts
@@ -266,6 +266,7 @@ export class KadDHT extends TypedEventEmitter<PeerDiscoveryEvents> implements Ka
       providers: this.providers,
       peerRouting: this.peerRouting,
       validators: this.validators,
+      validateUpdate: init.validateUpdate,
       logPrefix,
       metricsPrefix,
       datastorePrefix,
diff --git a/node_modules/@libp2p/kad-dht/src/rpc/handlers/put-value.ts b/node_modules/@libp2p/kad-dht/src/rpc/handlers/put-value.ts
index 156e65d..ad0473b 100644
--- a/node_modules/@libp2p/kad-dht/src/rpc/handlers/put-value.ts
+++ b/node_modules/@libp2p/kad-dht/src/rpc/handlers/put-value.ts
@@ -12,6 +12,7 @@ export interface PutValueHandlerInit {
   validators: Validators
   logPrefix: string
   datastorePrefix: string
+  validateUpdate?: (key: Uint8Array, existing: Uint8Array, incoming: Uint8Array) => Promise<void>
 }
 
 export interface PutValueHandlerComponents {
@@ -24,17 +25,19 @@ export class PutValueHandler implements DHTMessageHandler {
   private readonly validators: Validators
   private readonly log: Logger
   private readonly datastorePrefix: string
+  private readonly validateUpdate?: (key: Uint8Array, existing: Uint8Array, incoming: Uint8Array) => Promise<void>
 
-  constructor (components: PutValueHandlerComponents, init: PutValueHandlerInit) {
-    const { validators } = init
+  constructor(components: PutValueHandlerComponents, init: PutValueHandlerInit) {
+    const { validators, validateUpdate } = init
 
     this.components = components
     this.log = components.logger.forComponent(`${init.logPrefix}:rpc:handlers:put-value`)
     this.datastorePrefix = `${init.datastorePrefix}/record`
     this.validators = validators
+    this.validateUpdate = validateUpdate
   }
 
-  async handle (peerId: PeerId, msg: Message): Promise<Message> {
+  async handle(peerId: PeerId, msg: Message, ): Promise<Message> {
     const key = msg.key
     this.log('%p asked us to store value for key %b', peerId, key)
 
@@ -47,11 +50,27 @@ export class PutValueHandler implements DHTMessageHandler {
 
     try {
       const deserializedRecord = Libp2pRecord.deserialize(msg.record)
-
       await verifyRecord(this.validators, deserializedRecord)
-
       deserializedRecord.timeReceived = new Date()
       const recordKey = bufferToRecordKey(this.datastorePrefix, deserializedRecord.key)
+
+      // NEW: check existing record before overwriting
+      if (this.validateUpdate != null) {
+        let existingRaw: Uint8Array | undefined
+        try {
+          existingRaw = await this.components.datastore.get(recordKey)
+          const existing = Libp2pRecord.deserialize(existingRaw)
+          // throws if incoming version < existing version
+          await this.validateUpdate(deserializedRecord.key, existing.value, deserializedRecord.value)
+        } catch (err: any) {
+          if (err.message === 'stale record rejected') {
+            this.log('rejected stale record for %b (version too old)', key)
+            return existingRaw != null ? { ...msg, record: existingRaw } : msg // reject the PUT by echoing existing record
+          }
+          // datastore.get threw "not found" — no existing record, continue to store
+        }
+      }
+
       await this.components.datastore.put(recordKey, deserializedRecord.serialize().subarray())
       this.log('put record for %b into datastore under key %k', key, recordKey)
     } catch (err: any) {
diff --git a/node_modules/@libp2p/kad-dht/src/rpc/index.ts b/node_modules/@libp2p/kad-dht/src/rpc/index.ts
index a9a1491..7c83c35 100644
--- a/node_modules/@libp2p/kad-dht/src/rpc/index.ts
+++ b/node_modules/@libp2p/kad-dht/src/rpc/index.ts
@@ -26,6 +26,7 @@ export interface RPCInit {
   providers: Providers
   peerRouting: PeerRouting
   validators: Validators
+  validateUpdate?: (key: Uint8Array, existing: Uint8Array, incoming: Uint8Array) => Promise<void>
   logPrefix: string
   metricsPrefix: string
   datastorePrefix: string
