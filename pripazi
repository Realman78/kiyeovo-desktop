0. u stashu ti je enkripcija group info-a

0.5 U stashu ti je early return za dht queries (to su oni slucajevi kada je peer u konekciji ali ga se ne more dohvatit)

0.75 U kojim bucketima se jos vidi previse informacija cak i ako nemas encryption key?

1. [NUDGE][SEND_FAIL] peer=fz9zYUjU group=32398e08 reason=Remote closed connection during opening -> do libp2p je jednostavno. probal san fixat i zasad nemoren replicirat. zbrisat cu ovo ako se kad desi opet

4. DHT put error for /kiyeovo-fast-group-info-v/71e3f6bb-c60e-4dc7-b9cf-f16ad07b42a3/F9ntCrDHH8x8pdhLMgUsrJsjXyF-hCUE0M1TPylpiEo/29:
  from: PeerId(12D3KooWBEmK9VyRzDPpkhVdfH1LhpMTz4DH8NhA6e7pdi6AXstV),
  error: LimitedConnectionError: Cannot open protocol stream on limited connection

  - ovo gori se dogada jer se DHT stream po defaultu ne runna na limited connectionima. to bi trebalo u libp2p patchat
  - ovo necu zasad ali ako se desi da je previse konekcija limited i da se replikacija ne dogada, moralo bi se osposobit.
  - submittaj change request na github za ovo
  
6. nekakav rate limit na DHT zapise (na validatorima)

7. Stavi u README da se poveca SCRYPTN_N

8. OPTIMIZATION - protobuf umisto JSON-a za offline poruke

9. OPTIMIZATION: persistent stream per session

10. app security
- 1. sandbox: false (line 122)
    sandbox: false // Need to disable sandbox for IPC to work properly
    This comment is outdated. With modern Electron (v20+), contextIsolation: true + preload script works fine WITH sandbox enabled. Sandbox adds an extra layer of OS-level isolation. You should test with sandbox: true - it should work.

- 2. No CSP for production
  For production builds, you should add a CSP. Options:
  - Add <meta> tag in your index.html
  - Or set it programmatically via Electron session API
  Example strict CSP for production:
  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'">

- 3. DevTools in production? Line 139 opens DevTools in dev mode - that is fine. Just make sure isDev() correctly returns false in packaged builds.

11. Animations!

12. V2: // In-memory only for V1: if app restarts before retry, user must resend/ignore warning.
  // TODO: persist failed offline backup retries in DB if we need restart-safe retries.

13. if both users are online but it says that peer is unreachable, just send a message to each other so you connect. or maybe I should do that automatically? 

14. izgleda da se dugi register i fetch dese kada je jedan peer dostupan unutar connections-a, ali nedostupan za kontakt. pretpostavljan da je zbog toga ca je DHT promet preko limited connection-a zabranjen. obavezno cu probat overrideat taj parametar u libp2p kako bi dokazal hipotezu
  - I was wrong... ali neznan ca se onda dogada. ovo je crna kutija a nesmi bit... morat cu jos dublje istrazit kasnije